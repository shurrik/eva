<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.yunji.oa.dao.IprocessunitDAO">

    <sql id="Base_Column_List">
      id,
	  unitname unitname,
	  create_date createDate,
	  update_date updateDate
    </sql>
    
    <sql id="Query_condition">
      <trim prefix="WHERE" prefixOverrides="AND | OR">
              <if test="condition.unitname!=null">
			  	AND unitname Like '%${condition.unitname}%'
			  </if>
              <if test="condition.createDate!=null">
			  	AND create_date Like '%${condition.createDate}%'
			  </if>
              <if test="condition.updateDate!=null">
			  	AND update_date Like '%${condition.updateDate}%'
			  </if>
        </trim>
    </sql>
    
    <insert id="insert" parameterType="processunit">
        INSERT INTO t_oa_processunit (
	        id,
		    unitname,
		    create_date,
		    update_date
        ) VALUES (
         	#{id},
       		#{unitname},
       		sysdate(),
       		sysdate()
        )
    </insert>

    <!-- 更新 -->
   	<update id="update" parameterType="processunit">
        UPDATE t_oa_processunit
        <trim prefix="SET" suffixOverrides=",">
          <if test="unitname!=null">
           unitname=#{unitname},
          </if>
          <if test="createDate!=null">
           create_date=#{createDate},
          </if>
           update_date=sysdate(),
        </trim>
        WHERE
        id =#{id}
    </update>

   	<update id="updateMap">
        UPDATE t_oa_processunit
        <trim prefix="SET" suffixOverrides=",">
          <if test="map.unitname!=null">        
           unitname=#{map.unitname},
          </if>
          <if test="map.createDate!=null">        
           create_date=#{map.createDate},
          </if>
          <if test="map.updateDate!=null">        
           update_date=#{map.updateDate},
          </if>
        </trim>
        WHERE
       id = #{ map.id }
	</update>


     <insert id="insertMap" >
        INSERT INTO t_oa_processunit (
             	id,
			 	unitname,
			 	create_date,
			 	update_date,
        ) VALUES (
              	#{map.id},
		    	#{map.unitname},
		    	#{map.createDate},
		    	#{map.updateDate},
        )
    </insert>


  	<update id="updateNull" parameterType="processunit">
        UPDATE t_oa_processunit
        <trim prefix="SET" suffixOverrides=","> 
			    unitname=#{unitname},
			    create_date=#{createDate},
			    update_date=#{updateDate},
        </trim>
        WHERE
        id =#{id}
    </update>

   	<delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM t_oa_processunit
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND id=#{id}
        </trim>
    </delete>


 	<delete id="deleteByCondition" parameterType="java.util.Map">
        DELETE FROM  t_oa_processunit 
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="map.id!=null">
                AND id = #{map.id}
            </if>				        
			<if test="map.unitname!=null">
				AND unitname = #{map.unitname}
			</if>				
			<if test="map.createDate!=null">
				AND create_date = #{map.createDate}
			</if>				
			<if test="map.updateDate!=null">
				AND update_date = #{map.updateDate}
			</if>				
        </trim>
    </delete>

    <delete id="deleteByProperty" parameterType="java.util.Map">
        DELETE FROM t_oa_processunit WHERE
        ${property}=#{value}
    </delete>

    <select id="fetch" parameterType="java.lang.Integer" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND id =#{id}
        </trim>
    </select>

    <select id="findOne" parameterType="java.util.Map" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit WHERE
        ${property}=#{value} 
        LIMIT 0,1
    </select>

    <select id="findList" parameterType="java.util.Map" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit WHERE
       	${property} =#{value}
    </select>

    <select id="findAll" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit ORDER BY create_date ASC
    </select>

    <select id="queryPage" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit
        <include refid="Query_condition" />    
        <if test="condition.sort != null">
			order by ${condition.sort}
		</if>
        <if test="offset != null">
            limit ${offset}, ${rows}
        </if>
    </select>

    <select id="like" parameterType="java.util.Map" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit
        <trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="id!=null">
				AND id like '%#{id}%'
			</if>
			<if test="unitname!=null">
				AND unitname Like '%$ {condition.unitname}%'
			</if>
			<if test="createDate!=null">
				AND create_date Like '%$ {condition.createDate}%'
			</if>
			<if test="updateDate!=null">
				AND update_date Like '%$ {condition.updateDate}%'
			</if>
        </trim>
    </select>


 	<select id="queryList" parameterType="java.util.Map" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="map.id!=null">
                AND id = #{map.id}
            </if>
			<if test="map.unitname!=null">
				AND unitname = #{map.unitname}
			</if>
			<if test="map.createDate!=null">
				AND create_date = #{map.createDate}
			</if>
			<if test="map.updateDate!=null">
				AND update_date = #{map.updateDate}
			</if>
        </trim>
        ORDER BY ${orderBy} ${sortBy}
    </select>

   	<select id="queryOne" parameterType="java.util.Map" resultType="processunit">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_processunit
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="map.id!=null">
                AND id = #{map.id}
            </if>
			<if test="map.unitname!=null">
				AND unitname = #{map.unitname}
			</if>
			<if test="map.createDate!=null">
				AND create_date = #{map.createDate}
			</if>
			<if test="map.updateDate!=null">
				AND update_date = #{map.updateDate}
			</if>
        </trim>
        limit 0,1
    </select>

  	<select id="count" resultType="java.lang.Integer">
        SELECT count(id) FROM t_oa_processunit
        <include refid="Query_condition" />
    </select>

	<select id="selectMaxId" resultType="java.lang.Integer">
	    SELECT
	    MAX(id)
	    FROM t_oa_processunit
	</select>

</mapper>

