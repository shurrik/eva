<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.yunji.oa.dao.IAcessoriesDAO">

    <sql id="Base_Column_List">
      id,
	  file_type fileType,
	  file_id fileId,
	  file_url fileUrl,
	  file_name fileName,
	  create_date createDate,
	  update_date updateDate
    </sql>
    <sql id="Query_condition">
      <trim prefix="WHERE" prefixOverrides="AND | OR">
              <if test="condition.fileType!=null">
			  	AND file_type Like '%${condition.fileType}%'
			  </if>
              <if test="condition.fileId!=null">
			  	AND file_id Like '%${condition.fileId}%'
			  </if>
              <if test="condition.fileUrl!=null">
			  	AND file_url Like '%${condition.fileUrl}%'
			  </if>
              <if test="condition.fileName!=null">
			  	AND file_name Like '%${condition.fileName}%'
			  </if>
              <if test="condition.createDate!=null">
			  	AND create_date Like '%${condition.createDate}%'
			  </if>
              <if test="condition.updateDate!=null">
			  	AND update_date Like '%${condition.updateDate}%'
			  </if>
        </trim>         
    </sql>
    <insert id="insert" parameterType="Acessories">
        INSERT INTO t_oa_acessories (
	        id,
		    file_type,
		    file_id,
		    file_url,
		    file_name,
		    create_date,
		    update_date
        ) VALUES (
         	#{id},
       		#{fileType},
       		#{fileId},
       		#{fileUrl},
       		#{fileName},
       		#{createDate},
       		#{updateDate}
        )
    </insert>

    <!-- 更新 -->
   	<update id="update" parameterType="Acessories">
        UPDATE t_oa_acessories
        <trim prefix="SET" suffixOverrides=",">
          <if test="fileType!=null">
           file_type=#{fileType},
          </if>
          <if test="fileId!=null">
           file_id=#{fileId},
          </if>
          <if test="fileUrl!=null">
           file_url=#{fileUrl},
          </if>
          <if test="fileName!=null">
           file_name=#{fileName},
          </if>
          <if test="createDate!=null">
           create_date=#{createDate},
          </if>
          <if test="updateDate!=null">
           update_date=#{updateDate},
          </if>
        </trim>
        WHERE
        id =#{id}
    </update>

   	<update id="updateMap">
        UPDATE t_oa_acessories
        <trim prefix="SET" suffixOverrides=",">
          <if test="map.fileType!=null">        
           file_type=#{map.fileType},
          </if>
          <if test="map.fileId!=null">        
           file_id=#{map.fileId},
          </if>
          <if test="map.fileUrl!=null">        
           file_url=#{map.fileUrl},
          </if>
          <if test="map.fileName!=null">        
           file_name=#{map.fileName},
          </if>
          <if test="map.createDate!=null">        
           create_date=#{map.createDate},
          </if>
          <if test="map.updateDate!=null">        
           update_date=#{map.updateDate},
          </if>
        </trim>
        WHERE
       id = #{ map.id }
	</update>


     <insert id="insertMap" >
        INSERT INTO t_oa_acessories (
             	id,
			 	file_type,
			 	file_id,
			 	file_url,
			 	file_name,
			 	create_date,
			 	update_date,
        ) VALUES (
              	#{map.id},
		    	#{map.fileType},
		    	#{map.fileId},
		    	#{map.fileUrl},
		    	#{map.fileName},
		    	#{map.createDate},
		    	#{map.updateDate},
        )
    </insert>


  	<update id="updateNull" parameterType="Acessories">
        UPDATE t_oa_acessories
        <trim prefix="SET" suffixOverrides=","> 
			    file_type=#{fileType},
			    file_id=#{fileId},
			    file_url=#{fileUrl},
			    file_name=#{fileName},
			    create_date=#{createDate},
			    update_date=#{updateDate},
        </trim>
        WHERE
        id =#{id}
    </update>

   	<delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM t_oa_acessories
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND id=#{id}
        </trim>
    </delete>


 	<delete id="deleteByCondition" parameterType="java.util.Map">
        DELETE FROM  t_oa_acessories
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="map.id!=null">
                AND id = #{map.id}
            </if>				        
			<if test="map.fileType!=null">
				AND file_type = #{map.fileType}
			</if>				
			<if test="map.fileId!=null">
				AND file_id = #{map.fileId}
			</if>				
			<if test="map.fileUrl!=null">
				AND file_url = #{map.fileUrl}
			</if>				
			<if test="map.fileName!=null">
				AND file_name = #{map.fileName}
			</if>				
			<if test="map.createDate!=null">
				AND create_date = #{map.createDate}
			</if>				
			<if test="map.updateDate!=null">
				AND update_date = #{map.updateDate}
			</if>				
        </trim>
    </delete>

    <delete id="deleteByProperty" parameterType="java.util.Map">
        DELETE FROM t_oa_acessories WHERE
        ${property}=#{value}
    </delete>

    <select id="fetch" parameterType="java.lang.Integer" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND id =#{id}
        </trim>
    </select>

    <select id="findOne" parameterType="java.util.Map" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories WHERE
        ${property}=#{value} 
        LIMIT 0,1
    </select>

    <select id="findList" parameterType="java.util.Map" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories WHERE
       	${property} =#{value}
    </select>

    <select id="findAll" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories
    </select>

    <select id="queryPage" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories
      <include refid="Query_condition" />    
        <if test="condition.sort != null">
			order by ${condition.sort}
		</if>
        <if test="offset != null">
            limit ${offset}, ${rows}
        </if>
    </select>

    <select id="like" parameterType="java.util.Map" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories
        <trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="id!=null">
				AND id like '%#{id}%'
			</if>
			<if test="fileType!=null">
				AND file_type Like '%$ {condition.fileType}%'
			</if>
			<if test="fileId!=null">
				AND file_id Like '%$ {condition.fileId}%'
			</if>
			<if test="fileUrl!=null">
				AND file_url Like '%$ {condition.fileUrl}%'
			</if>
			<if test="fileName!=null">
				AND file_name Like '%$ {condition.fileName}%'
			</if>
			<if test="createDate!=null">
				AND create_date Like '%$ {condition.createDate}%'
			</if>
			<if test="updateDate!=null">
				AND update_date Like '%$ {condition.updateDate}%'
			</if>
        </trim>
    </select>


 	<select id="queryList" parameterType="java.util.Map" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="map.id!=null">
                AND id = #{map.id}
            </if>
			<if test="map.fileType!=null">
				AND file_type = #{map.fileType}
			</if>
			<if test="map.fileId!=null">
				AND file_id = #{map.fileId}
			</if>
			<if test="map.fileUrl!=null">
				AND file_url = #{map.fileUrl}
			</if>
			<if test="map.fileName!=null">
				AND file_name = #{map.fileName}
			</if>
			<if test="map.createDate!=null">
				AND create_date = #{map.createDate}
			</if>
			<if test="map.updateDate!=null">
				AND update_date = #{map.updateDate}
			</if>
        </trim>
        ORDER BY ${orderBy} ${sortBy}
    </select>

   	<select id="queryOne" parameterType="java.util.Map" resultType="Acessories">
        SELECT
        <include refid="Base_Column_List" />
        FROM t_oa_acessories
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="map.id!=null">
                AND id = #{map.id}
            </if>
			<if test="map.fileType!=null">
				AND file_type = #{map.fileType}
			</if>
			<if test="map.fileId!=null">
				AND file_id = #{map.fileId}
			</if>
			<if test="map.fileUrl!=null">
				AND file_url = #{map.fileUrl}
			</if>
			<if test="map.fileName!=null">
				AND file_name = #{map.fileName}
			</if>
			<if test="map.createDate!=null">
				AND create_date = #{map.createDate}
			</if>
			<if test="map.updateDate!=null">
				AND update_date = #{map.updateDate}
			</if>
        </trim>
        limit 0,1
    </select>

  	<select id="count" resultType="java.lang.Integer">
        SELECT count(id) FROM t_oa_acessories
        <include refid="Query_condition" />  
    </select>

	<select id="selectMaxId" resultType="java.lang.Integer">
	    SELECT
	    MAX(id)
	    FROM t_oa_acessories
	</select>

</mapper>

